(function () {
    'use strict';

    angular.module('stocksTrackerApp').controller('register', [
        'common',
        'chartsService',
        '$routeParams',
        register]);

    function register(common, chartsService, $routeParams) {
        
    }
})();