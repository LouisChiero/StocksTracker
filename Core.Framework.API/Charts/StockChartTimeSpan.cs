namespace Core.Framework.API.Charts
{
    /// <summary>
    /// Enumeration defines the time spans for a stock chart.
    /// </summary>
    public enum StockChartTimeSpan
    {
        OneDay = 0,
        FiveDays = 1,
        ThreeMonths = 2,
        SixMonths = 3,
        OneYear = 4,
        TwoYears = 5,
        FiveYears = 6,
        Maximum = 7
    }
}
